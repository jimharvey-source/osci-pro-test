{
  "name": "osci-pro-test",
  "version": "0.2.0",
  "private": true,
  "description": "OSCI Pro — the paid report build. Stripe checkout, stored runs, server-side PDF.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "pdfkit": "^0.15.2",
    "stripe": "^22.5.0"
  }
}
